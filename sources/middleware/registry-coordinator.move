module restaking::registry_coordinator{
    use aptos_std::bn254_algebra;
    use restaking::service_manager;
    use restaking::stake_registry;
    use restaking::bls_apk_registry;
    use restaking::index_registry;
}